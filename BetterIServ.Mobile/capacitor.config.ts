import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.leonhoppe.betteriserv',
  appName: 'BetterIServ',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
